[tools]
node = "26.7.0"
bun = "1.3.1"
actionlint = "1.7.12"
# The only source of the binary `bun run check:shell` runs, which reaches it
# through `mise exec`. Deleting this line turns that check off on every machine
# that has no shellcheck of its own.
shellcheck = "0.11.0"
